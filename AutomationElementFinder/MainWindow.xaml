<Window x:Class="AutomationElementFinder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:AutomationElementFinder="clr-namespace:AutomationElementFinder"
        Title="AutomationElementFinder"
        Height="500"
        Width="550">

    <DockPanel>
        <Border Background="YellowGreen" DockPanel.Dock="Bottom">
            <TextBlock Text="Hold down Ctrl-Shift and mouse over elements to select them" Margin="2" />
        </Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.4*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="0.6*" />
            </Grid.ColumnDefinitions>

            <DockPanel>
                <Button HorizontalAlignment="Left" Content="Toggle Highlight" Click="ToggleHighlight_Click" DockPanel.Dock="Bottom" />
                <TreeView x:Name="elementTree" ItemsSource="{Binding Elements}">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="{x:Type TreeViewItem}">
                            <Setter Property="IsExpanded" Value="True" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Padding" Value="2" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Childs}">
                            <TextBlock x:Name="PART_NameDisplay" Text="{Binding }" Foreground="Black" />
                            <HierarchicalDataTemplate.Triggers>
                                <DataTrigger Binding="{Binding HasAutomationId}" Value="True">
                                    <Setter TargetName="PART_NameDisplay" Property="Background" Value="LightGreen" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding HasAutomationId}" Value="False">
                                    <Setter TargetName="PART_NameDisplay" Property="Foreground" Value="Gray" />
                                    <Setter TargetName="PART_NameDisplay" Property="FontStyle" Value="Italic" />
                                </DataTrigger>
                            </HierarchicalDataTemplate.Triggers>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </DockPanel>

            <GridSplitter Width="4" ResizeBehavior="PreviousAndNext" Grid.Column="1" />

            <ScrollViewer Grid.Column="2">
                <AutomationElementFinder:AutomationElementVisualization Element="{Binding SelectedItem.AutomationElement, ElementName=elementTree}" />
            </ScrollViewer>

        </Grid>
    </DockPanel>
</Window>
